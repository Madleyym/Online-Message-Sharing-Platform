function showAlert() {
    // Display the alert message
    alert("Form submitted successfully!");
    // Optionally, you can prevent the form from actually submitting if needed
    return false; // Prevents default form submission
}